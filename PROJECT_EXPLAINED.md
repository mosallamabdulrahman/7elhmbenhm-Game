# دليل المعمارية الشامل وكيفية عمل كود لعبة "حيلهم بينهم" 🚀

هذا الملف يشرح لك بالتفصيل الدقيق وبكل وضوح كيف يعمل مشروع اللعبة من الصفر وحتى انتهاء المعركة، بما في ذلك كيفية جلب البيانات وعرضها، كيفية إنشاء الغرفة، وكيف يدير الحكم المعركة، مع شرح كامل لدور **TypeScript** و **Zustand** بعد التحديث الأخير.

---

## 📑 فهرس المحتويات

1. [المعمارية العامة والتقنيات المستخدمة](#1-المعمارية-العامة-والتقنيات-المستخدمة)
2. [نظام TypeScript: كيف يفهم الكود البيانات؟](#2-نظام-typescript-كيف-يفهم-الكود-البيانات)
3. [إدارة الحالات المركزية (Zustand Stores)](#3-إدارة-الحالات-المركزية-zustand-stores)
4. [دورة حياة اللعبة خطوة بخطوة (Game Lifecycle)](#4-دورة-حياة-اللعبة-خطوة-بخطوة-game-lifecycle)
   - [أولاً: جلب الفئات والمجموعات وعرضها في الواجهة](#أولاً-جلب-الفئات-والمجموعات-وعرضها-في-الواجهة)
   - [ثانياً: الكبسولة العائمة لاختيار الفئات (Seenjeem Dock)](#ثانياً-الكبسولة-العائمة-لاختيار-الفئات-seenjeem-dock)
   - [ثالثاً: إنشاء الغرفة وتوليد التوكنات وبنك الأسئلة](#ثالثاً-إنشاء-الغرفة-وتوليد-التوكنات-وبنك-الأسئلة)
   - [رابعاً: انضمام الفرق وتوزيع الجيوش (Deployment Phase)](#رابعاً-انضمام-الفرق-وتوزيع-الجيوش-deployment-phase)
   - [خامساً: شاشة المعركة التفاعلية ونظام الضربات (Battle Engine)](#خامساً-شاشة-المعركة-التفاعلية-ونظام-الضربات-battle-engine)
5. [محرك الصوت التخليقي ومنع التكرار (Audio Engine)](#5-محرك-الصوت-التخليقي-ومنع-التكرار-audio-engine)
6. [التواصل اللحظي وقاعدة البيانات (Supabase Realtime & RPCs)](#6-التواصل-اللحظي-وقاعدة-البيانات-supabase-realtime--rpcs)

---

## 1. المعمارية العامة والتقنيات المستخدمة

تم بناء المشروع باستخدام أحدث معايير الويب:

- **Next.js 15 (App Router)**: إطار العمل الأساسي، كل صفحة تقع في مسار داخل مجلد `app/` (مثل `app/battle/page.tsx`, `app/admin/page.tsx`).
- **TypeScript**: يمنع الأخطاء البرمجية قبل تشغيل الكود عبر تحديد نوع كل متغير ودالة وكائن بدقة 100%.
- **Zustand**: مخازن حالة خفيفة وسريعة جداً لإدارة منطق الإعداد والمعركة خارج مكونات الواجهة لتسهيل التعديل وسرعة الأداء.
- **Supabase (PostgreSQL + Realtime + RPCs)**: قاعدة البيانات السحابية المسؤولة عن حفظ الجلسات وتحديث شاشات اللاعبين والحكم لحظياً عبر تقنية WebSockets.
- **Web Audio API**: محرك صوتي تخليقي مدمج في المتصفح ينتج أصوات المعركة والألغام والمؤقت مباشرة بدون الحاجة لملفات mp3 ثقيلة.
- **Tailwind CSS**: لتنسيق الواجهات والتجاوب الكامل مع جميع الشاشات.

---

## 2. نظام TypeScript: كيف يفهم الكود البيانات؟

في السابق، عندما كان الكود جافاسكريبت عادي (`.jsx` / `.js`)، كان من الممكن كتابة `room.selected_categories.length` وإذا كانت `selected_categories` غير موجودة، تنهار الصفحة للمستخدم وتظهر شاشة بيضاء.

مع **TypeScript**، قمنا بإنشاء ملف تعريفات رئيسي يسمى [`types/game.ts`](./types/game.ts)، وهو مثل "عقد رسمي" بين أجزاء الكود:

### أمثلة على الأنواع المحددة في المشروع:

1. **`QuestionCategory` (فئة الأسئلة)**:
   ```typescript
   export interface QuestionCategory {
     id: string; // المعرف الفريد للفئة
     name: string; // اسم الفئة (مثلاً: علوم وطبيعة)
     image_url?: string | null; // رابط صورة الفئة
     group_id?: string | null; // المعرف الخاص بالمجموعة التي تتبعها
     is_active?: boolean; // هل الفئة مفعلة أم لا
   }
   ```
2. **`GameRoom` (بيانات الغرفة)**:
   ```typescript
   export interface GameRoom {
     id: string;
     status: "waiting" | "deployment" | "playing" | "finished" | "abandoned";
     judge_id?: string;
     selected_categories?: string[];
     active_question_id?: string | null;
     team_1_name?: string;
     team_2_name?: string;
     winner_team_index?: number | null;
   }
   ```
3. **`CombatEvent` (حدث القتال)**:
   ```typescript
   export interface CombatEvent {
     id: string;
     room_id: string;
     event_type: "strike" | "tool" | "reveal" | "system";
     actor_team_index?: number;
     target_team_index: number;
     cell_index: number;
     result?: "hit" | "miss" | "mine" | "blocked" | "pending";
     unit_type?:
       | "infantry"
       | "armored"
       | "tank"
       | "aircraft"
       | "submarine"
       | "mine"
       | null;
   }
   ```
   **الفائدة للمشروع**: المتصفح ومحرر الأكواد أصبحا يفهمان بالضبط ما هي الحقول الموجودة في كل استجابة من السيرفر، وتظهر لك رسائل خطأ فورية أثناء الكتابة تمنع أي ثغرات أو أخطاء تشغيلية.

---

## 3. إدارة الحالات المركزية (Zustand Stores)

بدلاً من تكديس كل البيانات والمتغيرات داخل الصفحة الواحدة، تم تنظيم اللعبة في مخزنين رئيسيين:

### 1. مخزن إعداد اللعبة [`stores/useGameSetupStore.ts`](./stores/useGameSetupStore.ts):

مسؤول عن:

- الفئات المحملة من Supabase (`categoriesList`).
- المجموعات التصنيفية (`groupsList`).
- الفئات الست التي اختارها المستخدم (`selectedCategories`).
- اسم اللعبة واسم الفريق الأول واسم الفريق الثاني.
- دالة بدء اللعبة وإنشاء الغرفة في قاعدة البيانات (`handleStartGame`).

### 2. مخزن المعركة الحية [`stores/useBattleStore.ts`](./stores/useBattleStore.ts):

مسؤول عن:

- حالة الغرفة الحالية (`room`).
- الفرق ومجموع النقاط والضربات المتوفرة (`teams`).
- أحداث القتال (`combatEvents`).
- المؤقت اللحظي للسؤال الحالي (`questionSeconds`).
- حالة المودالات المفتوحة (الرادار، لوحة الضرب، الفزعات).

---

## 4. دورة حياة اللعبة خطوة بخطوة (Game Lifecycle)

إليك الترتيب الزمني الكامل لما يحدث خلف الكواليس عند تشغيل اللعبة:

### أولاً: جلب الفئات والمجموعات وعرضها في الواجهة

1. عندما يفتح المستخدم الصفحة الرئيسية (`/`)، يقوم المكون [`components/home-page/GameSetupSection.tsx`](./components/home-page/GameSetupSection.tsx) باستدعاء دالة `loadSetup()` من مخزن Zustand.
2. تتوجه الدالة إلى ملف [`lib/game-data.ts`](./lib/game-data.ts) وتنفذ استعلامين لقاعدة بيانات Supabase:
   - سحب المجموعات المفعلة من جدول `category_groups`.
   - سحب فئات الأسئلة المفعلة من جدول `question_categories`.
3. تمر هذه الفئات عبر دالة تسمى `groupCategories()`:
   - تأخذ القائمة المسطحة من الفئات، وتقوم بفرز كل فئة ووضعها تحت "المجموعة" المناسبة لها (مثل وضع فئات الحيوانات والبحار تحت مجموعة "علوم وطبيعة").
4. يتم رسم المجموعات عبر مكون [`CategoryGroupSection.tsx`](./components/home-page/CategoryGroupSection.tsx)، حيث تظهر كل مجموعة بصندوق منظم قابل للطي والتوسيع يحمل شارة عنوانه البرتقالية.

---

### ثانياً: الكبسولة العائمة لاختيار الفئات (Seenjeem Dock)

- بمجرد أن ينقر المستخدم على أي فئة، يتم تشغيل دالة `toggleCategory(catId)`.
- إذا لم تكن الفئة مختارة، وكان عدد الفئات المختارة أقل من 6، يتم إضافتها للمصفوفة `selectedCategories`.
- **الكبسولة العائمة**:
  - **في شاشات الديسكتوب**: تظهر كبسولة بيضاء أنيقة مثبتة على يمين الشاشة (`fixed right-3 top-1/2`) تتحرك مع المستخدم أثناء السكرول.
  - **في شاشات الموبايل**: تظهر كبسولة أفقية مثبتة في أسفل الشاشة (`fixed bottom-3`).
  - تحتوي الكبسولة على 6 خانات، تظهر الفئات المختارة فيها بصورها المصغرة وشريط أسمائها البرتقالي مع زر (×) للإلغاء السريع.
  - في أسفل الكبسولة يوجد الزر الدائري البرتقالي ذو السهم للأسفل (`↓`)، والذي عند النقر عليه ينفذ `smooth scroll` سريعاً لأسفل الصفحة نحو الخطوة الثانية (تسمية الفرق).

---

### ثالثاً: إنشاء الغرفة وتوليد التوكنات وبنك الأسئلة

عندما يكتمل اختيار 6 فئات بدقة، ويقوم المستخدم بإدخال اسم اللعبة وأسماء الفرق، والضغط على زر **"أنشئ الغرفة وابدأ التحدي"**:

1. تتأكد الدالة `handleStartGame` أن المستخدم مسجل الدخول، وأن لديه رصيد ألعاب كافٍ، وأن 6 فئات بالضبط قد تم اختيارها.
2. يتم سحب أسئلة هذه الفئات الست عبر دالة `buildRoomQuestions()`:
   - تختار الخوارزمية 6 أسئلة لكل فئة (2 سهل + 2 وسط + 2 صعب) بحيث يتكون بنك الغرفة من **36 سؤالاً** موزعة بدقة على مصفوفة 6×6.
3. يتم إرسال طلب لـ Supabase لإنشاء صف في جدول `game_rooms` بحالة `"deployment"`.
4. يتم توليد توكن أمان عشوائي فريد لكل فريق (`team_1_token` و `team_2_token`):
   - هذا التوكن يسمح للاعبين بالانضمام مباشرة لمباراتهم عبر الرابط أو الباركود دون الحاجة لإنشاء حساب في الموقع!
5. يتم حفظ الأسئلة في جدول `room_questions_bank`، ويتم حفظ الفريقين في جدول `teams` برصيد ابتدائي 4000 نقطة.
6. تعرض الواجهة حاوية باركود أنيقة ومدمجة للفريقين، مع رابط مباشر لـ "شاشة الحكم".

---

### رابعاً: انضمام الفرق وتوزيع الجيوش (Deployment Phase)

1. يمسح لاعب الفريق الأول الباركود بهاتفه، فيفتح الرابط:
   `/battle?room_id=XYZ&team=1&token=TOKEN1`
2. يفتح كل فريق شاشة توزيع الجنود على خريطة بحرية مقاس 6×6 (36 مربعاً):
   - جندي (infantry)
   - مدرعة (armored)
   - دبابة (tank)
   - طائرة (aircraft)
   - غواصة (submarine)
   - لغم (mine)
3. يتابع الحكم من شاشته الحية حالة الفرق عبر كروت المتابعة (`○ قاعد يوزع جنوده` أو `✓ جاهز للمعركة`).
4. عندما ينهي الفريقان التوزيع، تتبدل حالة الغرفة في السيرفر تلقائياً إلى `"playing"`.

---

### خامساً: شاشة المعركة التفاعلية ونظام الضربات (Battle Engine)

تقع هذه الشاشة في [`components/battle/RefereeGameScreen.tsx`](./components/battle/RefereeGameScreen.tsx) وتتضمن:

1. **شبكة الأسئلة (6×6)**:
   - يختار الحكم سؤالاً، فيظهر السؤال لجميع الشاشات في نفس اللحظة عبر Supabase Realtime، ويبدأ العداد التنازلي التلقائي.
2. **عرض الإجابة النموذجية للحكم فقط**:
   - الحكم يرى نص الإجابة والصورة عبر دالة آمنة مشفرة (`get_question_answer`) لا يمكن للمتسابقين رؤيتها من هواتفهم.
3. **التحكيم ومنح الضربات**:
   - إذا أجاب الفريق الأول إجابة صحيحة، يضغط الحكم "إجابة صحيحة"، فيحصل الفريق على ضربة مدفعية.
4. **توجيه الضربة للخصم (`handleStrike`)**:
   - يفتح الحكم لوحة ضرب خريطة الخصم، ويضغط على المربع المطلوب استهدافه.
   - **استجابة فورية بدون تأخير**:
     1. فور النقر، يُشغّل المتصفح صوت القصف فوراً، ويتم تسجيل رقم المربع في سجل الموديول العام لمنع أي تكرار.
     2. يتم إرسال استدعاء لـ Supabase RPC يسمى `execute_strike`.
     3. تقوم الدالة في قاعدة البيانات بمطابقة المربع مع خريطة الخصم السرية:
        - إذا كان فيه جندي/دبابة/طائرة ➜ تصبح النتيجة **إصابة (Hit)** وتُخصم نقاط من الخصم.
        - إذا كان فارغاً ➜ تصبح النتيجة **طاش (Miss)**.
        - إذا كان فيه لغم ➜ تنفجر النتيجة **لغم (Mine)** وتُخصم نقاط مضاعفة.
        - إذا كان الخصم قد فعّل الدرع ➜ تصبح النتيجة **صد (Blocked)** ويحميه الدرع.

---

## 5. محرك الصوت التخليقي ومنع التكرار (Audio Engine)

في ملف [`hooks/useBattleAudio.ts`](./hooks/useBattleAudio.ts):

- **لماذا لا نستخدم ملفات MP3؟**:
  ملفات الـ MP3 تتأخر في التحميل وتحتاج إلى تنزيل عبر الشبكة مما يسبب تأخيراً في لحظة القصف، وتتكرر أحياناً إذا استجاب السيرفر مرتين.
- **كيف يعمل Web Audio API هنا؟**:
  المتصفح نفسه يقوم بتوليد الترددات الصوتية رياضياً في المعالج (Synthesizer):
  - **صوت الضربة القتالية (`playActionHitSound`)**: هبوط ترددي حاد من 240Hz إلى 45Hz عبر مذبذب Sawtooth مدمج بضوضاء بيضاء مفلترة تحاكي صوت القصف المدفعي المكتوم في 0.22 ثانية.
  - **صوت اللغم (`playMineSound`)**: انفجار عميق بتردد منخفض جداً (Sub-bass) من 150Hz إلى 25Hz مع رنين مكتوم يوضح خطورة اللغم.
- **ضمان عدم التكرار نهائياً**:
  يستخدم الكود متغيراً عاماً `globalPlayedStrikes = new Set<string>()`:
  - بمجرد أن يضرب الحكم خلية معينة، تُسجل إحداثياتها مثل `cell_2_14`.
  - إذا حاولت الـ WebSockets أو استجابة الـ HTTP تشغيل الصوت مرة أخرى، يفحص الكود السجل ويجدها مسجلة مسبقاً، فيتم إيقافها فوراً في 0 ميلي ثانية.

---

## 6. التواصل اللحظي وقاعدة البيانات (Supabase Realtime & RPCs)

السر في سرعة اللعبة وسلاستها هو الجمع بين:

1. **الـ PostgreSQL Database Functions (RPCs)**:
   - العمليات الحساسة (مثل فحص الإصابة، تفعيل الرادار، تفعيل الدرع، خصم النقاط) لا تتم في المتصفح، بل تُنفذ داخل دوال مشفرة في قاعدة البيانات لضمان عدم إمكانية الغش أو التلاعب بنتيجة المباراة.
2. **قنوات البث اللحظي (Realtime Channels)**:
   - الحكم واللاعبون مشتركون في قناة الغرفة `room:${roomId}`.
   - بمجرد أن تُدخل قاعدة البيانات أي صف في جدول `combat_events`، يرسل السيرفر إشعاراً فورياً لجميع الهواتف المتصلة لتحديث الخريطة والمربعات في أجزاء من الثانية.

---

## 💡 ملخص تدفق البيانات في سطر واحد:

> **قاعدة البيانات (Supabase)** ➜ **مخزن الإعداد (Zustand)** ➜ **الواجهة الأمامية والمجموعات والكبسولة العائمة** ➜ **إنشاء الغرفة والأسئلة (36 سؤال)** ➜ **توزيع الجيوش من الهواتف** ➜ **إدارة المعركة الحية من شاشة الحكم مع تحديث لحظي ومؤثرات صوتية فورية**.
